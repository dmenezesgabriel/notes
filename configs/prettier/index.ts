import type { Config } from 'prettier';

const config: Config = {
  singleQuote: true,
  semi: true,
  tabWidth: 2,
  trailingComma: 'all',
  arrowParens: 'always',
  printWidth: 100,
  endOfLine: 'lf',
  bracketSpacing: true,
  bracketSameLine: false,
};

export default config;
